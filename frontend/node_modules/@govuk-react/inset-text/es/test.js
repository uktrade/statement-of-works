import React from 'react';
import { mount } from 'enzyme';
import InsetTextTest, { InsetTextWithParagraph, testText } from './fixtures';
describe('inset text', function () {
  it('renders provided text', function () {
    var wrapper = mount(React.createElement(InsetTextTest, null));
    expect(wrapper.text()).toContain(testText);
  });
  it('matches the InsetText snapshot', function () {
    expect(mount(React.createElement(InsetTextTest, null))).toMatchSnapshot('inset text');
  });
  it('can render with a paragraph', function () {
    expect(mount(React.createElement(InsetTextWithParagraph, null))).toMatchSnapshot('inset text with paragraph');
  });
});
//# sourceMappingURL=test.js.map