import React from 'react';
import { mount } from 'enzyme';
import { ORANGE } from 'govuk-colours';
import Input from '.';
describe('Input', function () {
  var wrapper = React.createElement(Input, {
    type: "text"
  });
  var withError = React.createElement(Input, {
    type: "text",
    error: "example"
  });
  var withColouredError = React.createElement(Input, {
    type: "text",
    errorColor: ORANGE,
    error: "example"
  });
  it('matches wrapper snapshot', function () {
    expect(mount(wrapper)).toMatchSnapshot('wrapper mount');
  });
  it('matches withError snapshot', function () {
    expect(mount(withError)).toMatchSnapshot('with error mount');
  });
  it('matches withColouredError snapshot', function () {
    expect(mount(withColouredError)).toMatchSnapshot('with coloured error mount');
  });
});
//# sourceMappingURL=test.js.map