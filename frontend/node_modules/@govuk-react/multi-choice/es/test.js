import React from 'react';
import { mount } from 'enzyme';
import MultiChoice from '.';
import MultiChoiceWithKnobs, { MultiChoiceWithKnobsHint, MultiChoiceWithKnobsError } from './fixtures';
describe('MultiChoice', function () {
  it('renders with a label', function () {
    var wrapper = mount(React.createElement(MultiChoiceWithKnobs, null));
    expect(wrapper.find('LabelText').contains('Example label')).toBe(true);
  });
  it('renders with a hint', function () {
    var wrapper = mount(React.createElement(MultiChoiceWithKnobsHint, null));
    expect(wrapper.find('HintText').contains('Example hint')).toBe(true);
  });
  it('renders with an error', function () {
    var wrapper = mount(React.createElement(MultiChoiceWithKnobsError, null));
    expect(wrapper.find('ErrorText').contains('Example error')).toBe(true);
  });
  it('matches snapshot', function () {
    var metaData = {
      touched: true,
      error: 'Example error'
    };
    var wrapper = mount(React.createElement(MultiChoice, {
      label: "Example label",
      hint: "Example hint",
      meta: metaData
    }));
    expect(wrapper).toMatchSnapshot();
  });
});
//# sourceMappingURL=test.js.map