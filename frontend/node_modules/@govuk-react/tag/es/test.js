import React from 'react';
import { mount } from 'enzyme';
import Tag, { TagInactive } from './fixtures';
describe('Tag', function () {
  it('can render an inactive tag', function () {
    mount(React.createElement(TagInactive, null, "alpha"));
  });
  it('matches wrapper snapshot', function () {
    expect(mount(React.createElement(Tag, null, "beta"))).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map