// every export from @govuk-react/icons should be a React component that renders an SVG
// icons should be stateless therefore should be a function
import { mount } from 'enzyme';
import React from 'react';
import * as exports from '.';
describe('@govuk-react/icons', function () {
  // filter out private exports e.g. __esModule
  var componentNames = Object.keys(exports).filter(function (componentName) {
    return !componentName.startsWith('_');
  });

  var _loop = function _loop(n) {
    var key = componentNames[n];
    it("exports " + key + " as a React component that renders an SVG", function () {
      var Component = exports[key]; // TODO: this would fail if we wanted to export a constant that was `false`
      // not sure how to do "expect one of" in Jest so leaving for now
      // as it's possible we'd never need a false constant export

      expect(Component).toBeTruthy(); // icons should be stateless so we expect a function

      expect(typeof Component).toBe('function');
      expect(mount(React.createElement(Component, null)).find('svg')).toHaveLength(1);
    });
  };

  for (var n = 0; n < componentNames.length; n += 1) {
    _loop(n);
  }
});
//# sourceMappingURL=test.js.map