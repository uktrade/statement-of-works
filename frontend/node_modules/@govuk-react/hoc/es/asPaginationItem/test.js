import React from 'react';
import ReactDOM from 'react-dom';
import { shallow } from 'enzyme';
import asPaginationItem from '.';
var PaginationTag = asPaginationItem('a');
var wrapper = React.createElement(PaginationTag, {
  href: "https://example.com"
}, "Example");
describe('asPaginationItem', function () {
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(wrapper, div);
  });
  it('renders a component for each prop without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(React.createElement("div", null, React.createElement(PaginationTag, {
      href: "https://example.com",
      nextPage: true
    }, "Example"), React.createElement(PaginationTag, {
      href: "https://example.com",
      previousPage: true
    }, "Example"), React.createElement(PaginationTag, {
      href: "https://example.com",
      pageTitle: "This is an example"
    }, "Example")), div);
  });
  it('returns a component', function () {
    expect(shallow(wrapper).html()).toBeTruthy();
  });
  it('matches wrapper snapshot', function () {
    expect(shallow(wrapper)).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map