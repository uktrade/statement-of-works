// every export from @govuk-react/hoc should be
// - a function that takes a component and returns a component
import * as exports from '.';
describe('@govuk-react/hoc', function () {
  // filter out private exports e.g. __esModule
  var hocNames = Object.keys(exports).filter(function (hocName) {
    return !hocName.startsWith('_');
  });

  var _loop = function _loop(n) {
    var key = hocNames[n];
    it("exports " + key + " as a function", function () {
      var hoc = exports[key];
      expect(hoc).toBeTruthy();
      expect(typeof hoc).toBe('function');
    });
  };

  for (var n = 0; n < hocNames.length; n += 1) {
    _loop(n);
  }
});
//# sourceMappingURL=test.js.map