import React from 'react';
import ReactDOM from 'react-dom';
import { mount } from 'enzyme';
import ListNavigation from '.';
var example = 'example';
var wrapper = React.createElement(ListNavigation, null, example);
var wrapperMultiple = React.createElement(ListNavigation, null, example, example);
describe('ListNavigation', function () {
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(React.createElement(ListNavigation, null, example), div);
  });
  it('should render an unordered list', function () {
    var output = mount(wrapper);
    expect(output.find('ul')).toHaveLength(1);
    expect(mount(wrapperMultiple).find('ul li')).toHaveLength(2);
  });
  it('matches wrapper snapshot', function () {
    expect(mount(React.createElement(ListNavigation, {
      listStyleType: "square"
    }, "Example"))).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map