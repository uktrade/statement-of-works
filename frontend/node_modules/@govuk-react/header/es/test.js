function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import React from 'react';
import { mount } from 'enzyme';
import Header from '.';
import { H1, H2, H3, H4, H5, H6 } from './presets';
describe('Header', function () {
  // Capture console.warn and console.error, as this is deprecated and produces errors
  var OLD_ENV = process.env; // eslint-disable-next-line no-console

  var nativeWarn = console.warn; // eslint-disable-next-line no-console

  var nativeError = console.error;
  var warnCallCount;
  beforeEach(function () {
    jest.resetModules();
    process.env = _objectSpread({}, OLD_ENV); // eslint-disable-next-line no-console

    console.warn = function () {
      warnCallCount += 1;
    }; // eslint-disable-next-line no-console


    console.error = function () {};

    warnCallCount = 0;
  });
  afterEach(function () {
    // eslint-disable-next-line no-console
    console.warn = nativeWarn; // eslint-disable-next-line no-console

    console.error = nativeError;
    process.env = OLD_ENV;
  });
  it('produces deprecation warning', function () {
    process.env.NODE_ENV = 'development';
    mount(React.createElement(Header, null, "example"));
    mount(React.createElement(Header, {
      size: "SMALL"
    }, "Test"));
    mount(React.createElement(Header, {
      size: 16
    }, "Test"));
    mount(React.createElement(H1, null, "example"));
    mount(React.createElement(H2, null, "example"));
    mount(React.createElement(H3, null, "example"));
    mount(React.createElement(H4, null, "example"));
    mount(React.createElement(H5, null, "example"));
    mount(React.createElement(H6, null, "example"));
    expect(warnCallCount).not.toEqual(0);
  });
  it('throws an error if an unsupported size is used', function () {
    expect(function () {
      mount(React.createElement(Header, {
        size: 0
      }, "example"));
    }).toThrow();
    expect(function () {
      mount(React.createElement(Header, {
        size: 1
      }, "example"));
    }).toThrow();
    expect(function () {
      mount(React.createElement(Header, {
        size: 99999
      }, "example"));
    }).toThrow();
    expect(function () {
      mount(React.createElement(Header, {
        size: "test"
      }, "example"));
    }).toThrow();
  });
});
//# sourceMappingURL=test.js.map