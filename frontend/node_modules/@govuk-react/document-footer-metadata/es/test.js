import React from 'react';
import ReactDOM from 'react-dom';
import { mount } from 'enzyme';
import DocumentFooterMetadata from '.';
var example = ['example'];
var multiExample = ['example', 'exampleexample'];
var otherExample = [{
  id: 0,
  title: 'example',
  content: ['example']
}];
var wrapper = React.createElement(DocumentFooterMetadata, {
  from: example
});
var wrapperMultiple = React.createElement(DocumentFooterMetadata, {
  partOf: multiExample
});
var wrapperOther = React.createElement(DocumentFooterMetadata, {
  other: otherExample
});
describe('DocumentFooterMetadata', function () {
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(React.createElement(DocumentFooterMetadata, null, "Example"), div);
  });
  it('should render a from item', function () {
    var output = mount(wrapper);
    expect(output.find('ul')).toHaveLength(1);
  });
  it('should render multiple partof items', function () {
    expect(mount(wrapperMultiple).find('ul li')).toHaveLength(2);
  });
  it('should render a other items', function () {
    expect(mount(wrapperOther).find('ul li')).toHaveLength(1);
  });
  it('matches snapshot', function () {
    expect(mount(wrapperMultiple)).toMatchSnapshot('enzyme.mount');
  });
});
//# sourceMappingURL=test.js.map