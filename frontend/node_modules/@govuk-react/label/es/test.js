import React from 'react';
import ReactDOM from 'react-dom';
import { mount } from 'enzyme';
import Label from '.';
describe('Label', function () {
  var example = 'example';
  var wrapper = React.createElement(Label, null, example);
  var withError = React.createElement(Label, {
    error: example
  }, example);
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(wrapper, div);
  });
  it('matches wrapper snapshot', function () {
    expect(mount(wrapper)).toMatchSnapshot('wrapper mount');
  });
  it('matches withError snapshot', function () {
    expect(mount(withError)).toMatchSnapshot('with error mount');
  });
});
//# sourceMappingURL=test.js.map