import React from 'react';
import { mount } from 'enzyme';
import Example, { LinkWithAllOptionsOn } from './fixtures';
describe('Link', function () {
  it('basic view matches snapshot', function () {
    expect(mount(React.createElement(Example, null))).toMatchSnapshot('plain link');
  });
  it('all options view matches snapshot', function () {
    expect(mount(React.createElement(LinkWithAllOptionsOn, null))).toMatchSnapshot('link with all options');
  });
});
//# sourceMappingURL=test.js.map