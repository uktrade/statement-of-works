import React from 'react';
import { mount } from 'enzyme';
import Table, { TableSimple, TableWithCaption, TableWithHeadAndNumerics, TableWithCustomWidths } from './fixtures';
describe('Table', function () {
  it('renders without crashing', function () {
    mount(React.createElement(Table, null));
  });
  it('renders a simple example matching snapshot', function () {
    expect(mount(React.createElement(TableSimple, null))).toMatchSnapshot('simple table');
  });
  it('renders a table with a caption matching snapshot', function () {
    expect(mount(React.createElement(TableWithCaption, null))).toMatchSnapshot('table with caption');
  });
  it('renders a table with custom widths matching snapshot', function () {
    expect(mount(React.createElement(TableWithCustomWidths, null))).toMatchSnapshot('table with custom widths');
  });
  it('renders a table with head and numerics matching snapshot', function () {
    expect(mount(React.createElement(TableWithHeadAndNumerics, null))).toMatchSnapshot('table with head and numerics');
  });
});
//# sourceMappingURL=test.js.map