import React from 'react';
import { cleanup, fireEvent, render } from 'react-testing-library';
import { SimpleTabs } from './fixtures';
beforeAll(function () {
  Object.defineProperty(window, 'matchMedia', {
    // jsdom appears not to cope with CSSinJS media queries
    value: jest.fn(function () {
      return {
        matches: false
      };
    })
  });
});
afterEach(cleanup);
describe('Tabs', function () {
  it('clicking link works without crashing', function () {
    var _render = render(React.createElement(SimpleTabs, null)),
        container = _render.container;

    var firstLink = container.querySelector('a');
    fireEvent.click(firstLink);
    expect(container).toBeTruthy();
  });
  it('matches wrapper snapshot', function () {
    var _render2 = render(React.createElement(SimpleTabs, null)),
        asFragment = _render2.asFragment;

    expect(asFragment()).toMatchSnapshot();
  }); // TODO: test for clicking between tabs above media Queries width.
  // Not currently possible with jsdom
});
//# sourceMappingURL=test.js.map