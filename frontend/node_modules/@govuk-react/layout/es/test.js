import React from 'react';
import ReactDOM from 'react-dom';
import { mount } from 'enzyme';
import Layout from '.';
describe('Layout', function () {
  var props;
  var example = 'example';
  var wrapper = React.createElement(Layout, null, example);
  beforeEach(function () {
    props = {
      children: example
    };
  });
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(React.createElement(Layout, null, "Example"), div);
  });
  it('passes `props.children` to the rendered `wrapper` as `children`', function () {
    expect(wrapper.props.children).toBe(props.children);
  });
  it('matches wrapper snapshot', function () {
    expect(mount(wrapper)).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map