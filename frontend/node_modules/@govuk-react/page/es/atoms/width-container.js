var _styled;

// tracking https://github.com/alphagov/govuk-frontend/blob/master/src/objects/_width-container.scss
import styled from 'styled-components';
import { spacing } from '@govuk-react/lib';
import { GUTTER, GUTTER_HALF, MEDIA_QUERIES, SITE_WIDTH } from '@govuk-react/constants';
var WidthContainer = styled('div')((_styled = {
  maxWidth: SITE_WIDTH,
  margin: "0 " + GUTTER_HALF
}, _styled[MEDIA_QUERIES.TABLET] = {
  margin: "0 " + GUTTER
}, _styled[MEDIA_QUERIES.MAX] = {
  margin: '0 auto'
}, _styled), spacing.withWhiteSpace());
export default WidthContainer;
//# sourceMappingURL=width-container.js.map