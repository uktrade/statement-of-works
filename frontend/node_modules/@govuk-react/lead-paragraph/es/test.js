import React from 'react';
import ReactDOM from 'react-dom';
import { mount } from 'enzyme';
import LeadParagraph from '.';
describe('LeadParagraph', function () {
  var example = 'example';
  var wrapper = React.createElement(LeadParagraph, null, example);
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(React.createElement(LeadParagraph, null, "Example"), div);
  });
  it('matches wrapper snapshot', function () {
    expect(mount(wrapper)).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map