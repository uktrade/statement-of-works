// every export from constants should be a truthy (string, number, bool or plain non empty object)
// no functions, no undefined
import * as exports from '.';
var okTypes = ['number', 'string', 'boolean', 'object'];
describe('Constants', function () {
  var _loop = function _loop(n) {
    var key = Object.keys(exports)[n];
    it("exports " + key + " as valid type", function () {
      var value = exports[key];
      expect(okTypes.includes(typeof value)).toBe(true);
    });
  };

  for (var n = 0; n < Object.keys(exports).length; n += 1) {
    _loop(n);
  }
});
//# sourceMappingURL=test.js.map