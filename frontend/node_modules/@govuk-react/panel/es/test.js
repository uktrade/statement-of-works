import React from 'react';
import { mount } from 'enzyme';
import Panel from '.';
describe('Panel', function () {
  it('should render a panel without a body body', function () {
    var wrapper = mount(React.createElement(Panel, {
      title: "Example"
    }));
    expect(wrapper.text().includes('Example')).toBe(true);
  });
  it('should render a panel body that is a string', function () {
    var wrapper = mount(React.createElement(Panel, {
      title: "Example"
    }, "body"));
    expect(wrapper.text().includes('body')).toBe(true);
  });
  it('matches wrapper snapshot', function () {
    var wrapper = mount(React.createElement(Panel, {
      title: "Example"
    }, "body"));
    expect(wrapper).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map