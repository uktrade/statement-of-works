import React from 'react';
import { mount } from 'enzyme';
import { FormGroupDocumented as FormGroup } from '.';
describe('FormGroup', function () {
  it('matches snapshot', function () {
    expect(mount(React.createElement(FormGroup, null, "FormGroup example"))).toMatchSnapshot('FormGroup');
  });
  it('matches snapshot with error prop set', function () {
    expect(mount(React.createElement(FormGroup, {
      error: true
    }, "FormGroup example"))).toMatchSnapshot('with error prop');
  });
});
//# sourceMappingURL=test.js.map