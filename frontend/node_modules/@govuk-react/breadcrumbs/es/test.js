import React from 'react';
import { mount } from 'enzyme';
import Breadcrumbs from '.';
describe('breadcrumbs', function () {
  it('should render an item in ordered list', function () {
    var wrapper = mount(React.createElement(Breadcrumbs, null, "example"));
    expect(wrapper.find('ol')).toHaveLength(1);
  });
  it('should render multiple items in ordered list', function () {
    var wrapper = mount(React.createElement(Breadcrumbs, null, React.createElement(Breadcrumbs.Link, {
      href: "/section"
    }, "Section 1"), "example"));
    expect(wrapper.find('ol li')).toHaveLength(2);
  });
  it('should render an ordered list without ghost/duff children', function () {
    var wrapper = mount(React.createElement(Breadcrumbs, null, React.createElement(Breadcrumbs.Link, {
      href: "/section"
    }, "Section 1"), []
    /* empty node */
    , null
    /* null node */
    , "example"));
    expect(wrapper.find('ol li')).toHaveLength(2);
  });
  it('allows attributes to be set', function () {
    var wrapper = mount(React.createElement(Breadcrumbs, {
      id: "test",
      className: "test"
    }, "Crumb"));
    expect(wrapper.hasClass('test')).toBe(true);
    expect(wrapper.is('#test')).toBe(true);
  });
  it('matches snapshot', function () {
    var wrapper = mount(React.createElement(Breadcrumbs, null, React.createElement(Breadcrumbs.Link, {
      href: "/section"
    }, "Section 1"), React.createElement(Breadcrumbs.Link, {
      href: "/section2"
    }, "Section 2")));
    expect(wrapper).toMatchSnapshot('breadcrumbs');
  });
});
//# sourceMappingURL=test.js.map