import styled from 'styled-components';
import { YELLOW, BLACK } from 'govuk-colours';
import { asTopNavAnchor } from '..';

var asNavLinkAnchor = function asNavLinkAnchor(AnchorType) {
  var Anchor = asTopNavAnchor(AnchorType);
  var StyledNavLinkAnchor = styled(Anchor)({
    display: 'inline-block',
    ':focus': {
      color: BLACK,
      backgroundColor: YELLOW,
      outline: "3px solid " + YELLOW,
      ':hover': {
        borderBottomColor: BLACK
      }
    }
  });
  return StyledNavLinkAnchor;
};

export default asNavLinkAnchor;
//# sourceMappingURL=asNavLinkAnchor.js.map