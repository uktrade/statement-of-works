import React from 'react';
import { mount } from 'enzyme';
import WarningText from './fixtures';
describe('WarningText', function () {
  var exampleText = 'A very long warning message. This includes a lot of text to give a good representation of a more average length warning. That way you can see more than one line wrapping.';
  var wrapper;
  it('renders without crashing', function () {
    wrapper = mount(React.createElement(WarningText, null, exampleText));
  });
  it('renders an SVG', function () {
    expect(wrapper.find('svg')).toHaveLength(1);
  });
  it('renders expected strong text', function () {
    expect(wrapper.find('strong').text()).toBe(exampleText);
  });
  it('to match snapshot', function () {
    expect(wrapper).toMatchSnapshot();
  });
});
//# sourceMappingURL=test.js.map