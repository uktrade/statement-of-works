import React from 'react';
import { mount } from 'enzyme';
import { DetailsSimple, summaryText, paragraphs } from './fixtures';
describe('details', function () {
  it('should render the provided text and content', function () {
    var wrapper = mount(React.createElement(DetailsSimple, null));
    expect(wrapper.text()).toContain(summaryText);
    expect(wrapper.text()).toContain(paragraphs[0]);
  });
  it('matches the Details snapshot', function () {
    expect(mount(React.createElement(DetailsSimple, null))).toMatchSnapshot('details');
  });
});
//# sourceMappingURL=test.js.map