import React from 'react';
import { mount } from 'enzyme';
import { VisuallyHiddenDocumented as VisuallyHidden } from '.';
describe('VisuallyHidden', function () {
  it('matches snapshot', function () {
    expect(mount(React.createElement(VisuallyHidden, null, "Example"))).toMatchSnapshot('default render');
  });
  it('focusable version matches snapshot', function () {
    expect(mount(React.createElement(VisuallyHidden, {
      focusable: true
    }, "Example"))).toMatchSnapshot('focusable');
  });
  it('with important={false} matches snapshot', function () {
    expect(mount(React.createElement(VisuallyHidden, {
      important: false
    }, "Example"))).toMatchSnapshot('not important');
  });
});
//# sourceMappingURL=test.js.map