import React from 'react';
import ReactDOM from 'react-dom';
import { shallow, mount } from 'enzyme';
import Checkbox from '.';
describe('Checkbox', function () {
  var example = 'example';
  var wrapper = React.createElement(Checkbox, null, example);
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(wrapper, div);
  });
  it('should render an input type="checkbox"', function () {
    var output = shallow(wrapper);
    expect(output.find('input[type="checkbox"]')).toBeTruthy();
  });
  it('should render a label', function () {
    var output = shallow(wrapper);
    expect(output.find('label')).toBeTruthy();
  });
  it('matches wrapper snapshot', function () {
    expect(mount(wrapper)).toMatchSnapshot('wrapper mount');
  });
  it('renders disabled checkbox', function () {
    expect(mount(React.createElement(Checkbox, {
      disabled: true
    }, "Example"))).toMatchSnapshot('disabled');
  });
  it('can render with hint text', function () {
    expect(mount(React.createElement(Checkbox, {
      hint: "Hint text"
    }, "Example with hint text"))).toMatchSnapshot('hint text');
  });
});
//# sourceMappingURL=test.js.map