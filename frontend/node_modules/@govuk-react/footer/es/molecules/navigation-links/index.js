import React from 'react';
import PropTypes from 'prop-types';
import Section from '../../atoms/section';
import SectionHeading from '../../atoms/section-heading';
import UnorderedList from '../../atoms/unordered-list';
import ListItem from '../../atoms/list-item';

var NavigationLinks = function NavigationLinks(_ref) {
  var heading = _ref.heading,
      listColumns = _ref.listColumns,
      children = _ref.children;
  return React.createElement(Section, null, React.createElement(SectionHeading, {
    size: "MEDIUM"
  }, heading), children && React.createElement(UnorderedList, {
    columns: listColumns
  }, children.length && children.map ? children.map(function (child, i) {
    return child && (child.length || child.props) ? React.createElement(ListItem, {
      key: child.key || i
    }, child) : null;
  }) : React.createElement(ListItem, null, children)));
};

NavigationLinks.defaultProps = {
  listColumns: 0
};
export default NavigationLinks;
//# sourceMappingURL=index.js.map