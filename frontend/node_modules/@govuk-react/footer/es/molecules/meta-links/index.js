import React from 'react';
import PropTypes from 'prop-types';
import { H2 } from '@govuk-react/heading';
import VisuallyHidden from '@govuk-react/visually-hidden';
import UnorderedList from '../../atoms/unordered-list';
import ListItem from '../../atoms/list-item';

var MetaLinks = function MetaLinks(_ref) {
  var children = _ref.children,
      heading = _ref.heading;
  return React.createElement(React.Fragment, null, React.createElement(VisuallyHidden, null, React.createElement(H2, null, heading)), children && React.createElement(UnorderedList, {
    inline: true
  }, children.length && children.map ? children.map(function (child, i) {
    return child && (child.length || child.props) ? React.createElement(ListItem, {
      key: child.key || i,
      inline: true
    }, child) : null;
  }) : React.createElement(ListItem, {
    inline: true
  }, children)));
};

export default MetaLinks;
//# sourceMappingURL=index.js.map