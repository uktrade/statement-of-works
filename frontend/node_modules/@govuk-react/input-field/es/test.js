import React from 'react';
import ReactDOM from 'react-dom';
import { shallow, mount } from 'enzyme';
import InputField from '.';
describe('InputField', function () {
  var example = 'example';
  var wrapper = React.createElement(InputField, null, example);
  var meta = {
    touched: true,
    error: example
  };
  it('renders without crashing', function () {
    var div = document.createElement('div');
    ReactDOM.render(React.createElement(InputField, {
      hint: example,
      meta: meta
    }, example), div);
  });
  it('should render an input type="text"', function () {
    var output = shallow(wrapper);
    expect(output.find('input[type="text"]')).toBeTruthy();
  });
  it('should render a label', function () {
    var output = shallow(wrapper);
    expect(output.find('label')).toBeTruthy();
  });
  it('matches wrapper snapshot', function () {
    expect(mount(wrapper)).toMatchSnapshot('wrapper mount');
  });
});
//# sourceMappingURL=test.js.map